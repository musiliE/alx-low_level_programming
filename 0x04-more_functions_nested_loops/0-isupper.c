#include "main.h"
/**
 *main - main function
 *isupper - checks for uppercase character
 *Return - 1 if c is uppercase, 0 otherwise
 *
 */
int main(void);

{
	char c;

	c = 'C';

	if (isupper(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return ();
}
