#include "main.h"
/**
 *main - main function
 *isupper - checks for uppercase character
 *Return - 1 if c is uppercase, 0 otherwise
 *
 */
int _isupper(int c);

{
	c = 'A';

	if (isupper(c));
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
