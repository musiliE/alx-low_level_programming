#include "main.h"
/**
 *isupper - checks for uppercase character c
 *Return - 1 if c is uppercase, 0 otherwise
 */
int _isupper(int c);

{
	char ch = 'C';

	if (isupper(ch))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
